# Agent instructions

The authoritative agent/contributor guidance for this package lives in
[CLAUDE.md](CLAUDE.md). Please read it: it covers the architecture (catalog →
self-updating index → download/cache → load → reconstruction extensions), the
build/test/format commands, the TestItems tag scheme (and how the `:network` and
`:mrt` tests are gated), and the data-licensing requirements.
